# Aider configuration — routes through LiteLLM homelab proxy
# Set the API key in your shell profile:
#   export OPENAI_API_KEY=<litellm-master-key>

openai-api-base: http://localhost:4000/v1
model: openai/gpt-4o
